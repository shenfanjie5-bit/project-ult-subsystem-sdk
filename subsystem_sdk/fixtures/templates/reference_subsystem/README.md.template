# $package_name

Reference subsystem skeleton for `$subsystem_id`.

The generated package exposes placeholder handlers for Ex-1, Ex-2, and Ex-3,
plus a context factory and smoke entrypoint wired to SDK fixture bundles.
